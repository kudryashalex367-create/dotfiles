// ╔══════════════════════════════════════════════════════════════╗
// ║          NIRI CONFIG — FIXED MONOCHROME OBSIDIAN             ║
// ╚══════════════════════════════════════════════════════════════╝

// ── Переменные Окружения (Курсор и Прокси Hiddify) ───────────────
environment {
    XCURSOR_THEME "Bibata-Modern-Classic"
    XCURSOR_SIZE "24"
    
    // Прокси Hiddify (порт 12334)
    http_proxy "http://127.0.0.1:12334"
    https_proxy "http://127.0.0.1:12334"
    all_proxy "socks5://127.0.0.1:12334"
    HTTP_PROXY "http://127.0.0.1:12334"
    HTTPS_PROXY "http://127.0.0.1:12334"
    ALL_PROXY "socks5://127.0.0.1:12334"
}

// ── Тема Курсора ───────────────────────────────────────────────
cursor {
    xcursor-theme "Bibata-Modern-Ice"
    xcursor-size 24
}

// ── Настройка Монитора (144 Гц) ─────────────────────────────────
// Проверить имя выхода можно командой: niri msg outputs
output "DP-2" {
    mode "1920x1080@144.000"
}
output "HDMI-A-1" {
    mode "1920x1080@144.000"
}
output "eDP-1" {
    mode "1920x1080@144.000"
}

// ── Ввод и Раскладка Клавиатуры ─────────────────────────────────
input {
    keyboard {
        xkb {
            layout "us,ru"
            options "grp:win_space_toggle"
        }
    }

    touchpad {
        tap
        natural-scroll
        accel-speed 0.2
    }

    mouse {
        accel-speed -0.4
    }
}

// ── Настройки Отображения ───────────────────────────────────────
layout {
    gaps 10

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }

    default-column-width { proportion 0.5; }

    focus-ring {
        off
    }

    border {
        width 2
        active-color "#ffffff"
        inactive-color "#27272a"
    }

    struts {
        top 0
        bottom 0
        left 0
        right 0
    }
}

// ── Правила для окон ────────────────────────────────────────────
window-rule {
    geometry-corner-radius 16
    clip-to-geometry true
}

prefer-no-csd

// ── Автозапуск Сервисов, Прокси и Курсора ───────────────────────
spawn-at-startup "swaync"
spawn-at-startup "swaybg" "-i" "/home/zev1ce/Загрузки/wallhaven-gw55w7.png" "-m" "fill"
spawn-at-startup "/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1"
spawn-at-startup "wl-paste" "--watch" "cliphist" "store"
spawn-at-startup "gnome-keyring-daemon" "--start" "--components=secrets"
spawn-at-startup "swaybg" "-i" "/home/zev1ce/Загрузки/wallhaven-gw55w7.png" "-m" "fill"
// Автопрописка системного прокси Hiddify в GNOME
spawn-at-startup "gsettings" "set" "org.gnome.system.proxy" "mode" "manual"
spawn-at-startup "gsettings" "set" "org.gnome.system.proxy.http" "host" "127.0.0.1"
spawn-at-startup "gsettings" "set" "org.gnome.system.proxy.http" "port" "12334"
spawn-at-startup "gsettings" "set" "org.gnome.system.proxy.https" "host" "127.0.0.1"
spawn-at-startup "gsettings" "set" "org.gnome.system.proxy.https" "port" "12334"
spawn-at-startup "gsettings" "set" "org.gnome.system.proxy.socks" "host" "127.0.0.1"
spawn-at-startup "gsettings" "set" "org.gnome.system.proxy.socks" "port" "12334"
spawn-at-startup "xwayland-satellite"

// Установка курсора Bibata для всех GTK приложений
spawn-at-startup "gsettings" "set" "org.gnome.desktop.interface" "cursor-theme" "Bibata-Modern-Classic"
spawn-at-startup "gsettings" "set" "org.gnome.desktop.interface" "cursor-size" "24"

screenshot-path "~/Pictures/Screenshots/Screenshot_%Y-%m-%d_%H-%M-%S.png"

// ── Горячие Клавиши (Binds) ─────────────────────────────────────
binds {
    // Основные приложения и утилиты
    Mod+Return { spawn "kitty"; }
    Mod+D { spawn "fuzzel"; }
    Mod+Q { close-window; }
    Mod+Shift+E { quit; }
    Mod+L { spawn "sh" "-c" "niri msg action switch-layout 0 && hyprlock"; }
    Mod+N { spawn "swaync-client" "-t" "-sw"; }
    Mod+V { spawn "bash" "-c" "cliphist list | fuzzel -d | cliphist decode | wl-copy"; }

    // Обзор (Overview) и Окно Подсказок
    Mod+O { toggle-overview; }
    Mod+Slash { show-hotkey-overlay; }

    // Навигация по колонкам (Влево / Вправо)
    Mod+Left  { focus-column-left; }
    Mod+Right { focus-column-right; }
    Mod+Shift+Left  { move-column-left; }
    Mod+Shift+Right { move-column-right; }

    // Навигация по Рабочим Столам (Вверх / Вниз на Win + Стрелки)
    Mod+Down { focus-workspace-down; }
    Mod+Up   { focus-workspace-up; }

    // Перемещение колонок на Рабочий Стол (Вверх / Вниз)
    Mod+Shift+Down { move-column-to-workspace-down; }
    Mod+Shift+Up   { move-column-to-workspace-up; }

    // Управление шириной и фокусировкой
    Mod+R { switch-preset-column-width; }
    Mod+F { maximize-column; }
    Mod+Shift+F { fullscreen-window; }
    Mod+C { center-column; }

    Mod+Minus { consume-window-into-column; }
    Mod+Equal { expel-window-from-column; }

    // Прямой переход по номерам рабочих столов (1..5)
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }

    Mod+Shift+1 { move-column-to-workspace 1; }
    Mod+Shift+2 { move-column-to-workspace 2; }
    Mod+Shift+3 { move-column-to-workspace 3; }
    Mod+Shift+4 { move-column-to-workspace 4; }
    Mod+Shift+5 { move-column-to-workspace 5; }

    // Мультимедиа и громкость (Восстановленные полные команды)
    XF86AudioRaiseVolume allow-when-locked=true { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%+"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%-"; }
    XF86AudioMute        allow-when-locked=true { spawn "wpctl" "set-mute" "@DEFAULT_AUDIO_SINK@" "toggle"; }
    XF86MonBrightnessUp   allow-when-locked=true { spawn "brightnessctl" "set" "10%+"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "brightnessctl" "set" "10%-"; }

    // Скриншоты
    Ctrl+Shift+S { screenshot; }
    Ctrl+Print { screenshot-screen; }
    Alt+Print { screenshot-window; }
}

window-rule {
    match app-id="^steam$"
    default-column-width { proportion 0.75; }
}

window-rule {
    // Всплывающие окна Steam (список друзей, настройки) делаем плавающими, если нужно
    match title="^Friends List$"
    open-floating true
}
